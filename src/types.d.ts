export interface LoginFormValues {
    email: string;
    password: string;
}

export interface NewsFormValues {
    id?: string
    title: string
    description: string
    source: string 
    image: string
}